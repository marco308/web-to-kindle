services:
  web-to-kindle:
    build: .
    ports:
      - "3001:3001"
    environment:
      - SCREENSHOT_URL=${SCREENSHOT_URL:-https://www.bbc.co.uk/news}
      - KINDLE_WIDTH=${KINDLE_WIDTH:-1072}
      - KINDLE_HEIGHT=${KINDLE_HEIGHT:-1448}
      - PORT=3001
    restart: unless-stopped
